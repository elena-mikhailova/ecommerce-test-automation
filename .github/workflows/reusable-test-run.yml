name: Reusable Test Run

on:
  workflow_call:
    inputs:
      groups:
        description: JUnit test group to run
        required: true
        type: string

      exclude_quarantine:
        description: Exclude quarantined tests
        required: false
        default: true
        type: boolean

      allow_failure:
        description: Allow test failures without failing the job
        required: false
        default: false
        type: boolean

      notify_slack:
        description: Send test results to Slack
        required: false
        default: false
        type: boolean

      browser:
        description: Browser for UI tests
        required: false
        default: chrome
        type: string

    secrets:
      slack_webhook_url:
        description: Slack incoming webhook URL
        required: false

jobs:
  tests:
    name: ${{ inputs.groups }} tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Check test environment
        shell: bash
        run: |
          STATUS=$(curl -L -sS \
            --connect-timeout 10 \
            --max-time 30 \
            -o /dev/null \
            -w "%{http_code}" \
            https://automationexercise.com/products) || STATUS="000"
          
          echo "Environment HTTP status: $STATUS"
          
          if [ "$STATUS" != "200" ]; then
            echo "::error::Test environment is unavailable. HTTP status: $STATUS"
            exit 1
          fi

      - name: Set up Java
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: '25'
          cache: maven

      - name: Run tests without quarantine
        if: ${{ inputs.exclude_quarantine == true }}
        continue-on-error: ${{ inputs.allow_failure }}
        run: >
          ./mvnw clean test
          -Dgroups=${{ inputs.groups }}
          -Dselenide.browser=${{ inputs.browser }}
          -DexcludedGroups=quarantine
          -Dselenide.headless=true

      - name: Run tests
        if: ${{ inputs.exclude_quarantine == false }}
        continue-on-error: ${{ inputs.allow_failure }}
        run: >
          ./mvnw clean test
          -Dgroups=${{ inputs.groups }}
          -Dselenide.browser=${{ inputs.browser }}
          -Dselenide.headless=true

      - name: Publish JUnit test report
        id: junit-report
        if: ${{ always() && hashFiles('**/target/surefire-reports/TEST-*.xml') != '' }}
        continue-on-error: ${{ inputs.allow_failure }}
        uses: step-security/action-junit-report@v6
        with:
          report_paths: '**/target/surefire-reports/TEST-*.xml'
          check_name: '${{ inputs.groups }} Test Report - ${{ inputs.browser }}'
          detailed_summary: true
          include_passed: false
          fail_on_parse_error: true
          require_tests: true

      - name: Upload Surefire reports
        if: ${{ always() && hashFiles('target/surefire-reports/**') != '' }}
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          name: surefire-reports-${{ inputs.groups }}-${{ inputs.browser }}
          path: target/surefire-reports/
          retention-days: 14

      - name: Upload UI failure artifacts
        if: ${{ always() && hashFiles('build/reports/tests/**') != '' }}
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          name: ui-failure-artifacts-${{ inputs.groups }}-${{ inputs.browser }}
          path: build/reports/tests/
          retention-days: 14

      - name: Upload Allure results
        if: ${{ always() && hashFiles('target/allure-results/**') != '' }}
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          name: allure-results-${{ inputs.groups }}-${{ inputs.browser }}
          path: target/allure-results/
          retention-days: 14

      - name: Generate Allure report
        if: ${{ always() && hashFiles('target/allure-results/**') != '' }}
        continue-on-error: true
        run: ./mvnw allure:report

      - name: Upload Allure report
        if: ${{ always() && hashFiles('target/site/allure-maven-plugin/**') != '' }}
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          name: allure-report-${{ inputs.groups }}-${{ inputs.browser }}
          path: target/site/allure-maven-plugin/
          retention-days: 14

      - name: Notify Slack
        if: ${{ always() && inputs.notify_slack && hashFiles('**/target/surefire-reports/TEST-*.xml') != '' }}
        uses: slackapi/slack-github-action@v4.0.0
        with:
          webhook: ${{ secrets.slack_webhook_url }}
          webhook-type: incoming-webhook
          payload: |
            text: "${{ steps.junit-report.outputs.failed != '0' && '❌' || '✅' }} ${{ inputs.groups }} tests: ${{ steps.junit-report.outputs.failed != '0' && 'FAILED' || 'PASSED' }}"
            blocks:
              - type: "section"
                text:
                  type: "mrkdwn"
                  text: "*${{ inputs.groups }} tests* — ${{ steps.junit-report.outputs.failed != '0' && '❌ FAILED' || '✅ PASSED' }}"
              - type: "section"
                fields:
                  - type: "mrkdwn"
                    text: "*Passed:*\n${{ steps.junit-report.outputs.passed }}"
                  - type: "mrkdwn"
                    text: "*Failed:*\n${{ steps.junit-report.outputs.failed }}"
                  - type: "mrkdwn"
                    text: "*Skipped:*\n${{ steps.junit-report.outputs.skipped }}"
                  - type: "mrkdwn"
                    text: "*Total:*\n${{ steps.junit-report.outputs.total }}"
              - type: "section"
                text:
                  type: "mrkdwn"
                  text: "*Repository:* ${{ github.repository }}\n*Branch:* ${{ github.ref_name }}\n*Non-blocking:* ${{ inputs.allow_failure }}"
              - type: "actions"
                elements:
                  - type: "button"
                    text:
                      type: "plain_text"
                      text: "Open GitHub Actions"
                    url: "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"